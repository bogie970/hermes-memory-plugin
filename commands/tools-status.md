---
description: Show current tool lifecycle state — loaded tools, scores, pins, budget cap status
---

Run the tool lifecycle observer over the current session transcript
and report what's loaded, current scores, pin sources, and whether
the budget caps are at risk.

!python -m aisys.tools.status_cli
